import React,{useEffect,useMemo,useState}from"react";import{useOutletContext}from"react-router-dom";import{ArrowLeft,ExternalLink,Play,Search,Tv}from"lucide-react";import{LIVE_TV_DEFAULTS,type LiveTvChannel}from"../data/liveTvDefaults";
type CmsResponse={cms?:{liveTvChannels?:unknown}};const normalize=(value:unknown):LiveTvChannel[]=>Array.isArray(value)?value.filter((x:any)=>x?.id&&x?.name&&x?.url).map((x:any,i)=>({...x,enabled:x.enabled!==false,order:Number.isFinite(x.order)?x.order:i})).sort((a,b)=>(a.order??0)-(b.order??0)):[];const REMOVED_UNVERIFIED_CHANNELS=new Set(["ani","ians"]);const isAllowedChannel=(channel:LiveTvChannel)=>!REMOVED_UNVERIFIED_CHANNELS.has(channel.name.trim().toLowerCase());
export default function LiveTV(){const{lang}=useOutletContext<{lang:"en"|"hi"}>();const hi=lang==="hi";const[search,setSearch]=useState("");const[channels,setChannels]=useState<LiveTvChannel[]>(LIVE_TV_DEFAULTS.filter(isAllowedChannel));const[active,setActive]=useState<LiveTvChannel|null>(null);const[serverControlled,setServerControlled]=useState(false);useEffect(()=>{let cancelled=false;fetch("/api/cms",{cache:"no-store"}).then(r=>r.ok?r.json()as Promise<CmsResponse>:null).then(data=>{const configured=data?.cms?.liveTvChannels;if(!cancelled&&Array.isArray(configured)){setChannels(normalize(configured).filter(isAllowedChannel));setServerControlled(true)}}).catch(()=>undefined);return()=>{cancelled=true}},[]);useEffect(()=>{const onPopState=()=>setActive(null);window.addEventListener("popstate",onPopState);return()=>window.removeEventListener("popstate",onPopState)},[]);const visible=useMemo(()=>channels.filter(c=>c.enabled!==false),[channels]);const filtered=useMemo(()=>{const q=search.trim().toLowerCase();return q?visible.filter(c=>`${c.name} ${c.category}`.toLowerCase().includes(q)):visible},[search,visible]);const videoId=active?.videoId||active?.url.match(/(?:youtu\.be\/|youtube\.com\/(?:live\/|watch\?v=))([^?&/]+)/)?.[1];const embedUrl=videoId?`https://www.youtube.com/embed/${videoId}?autoplay=1&playsinline=1&rel=0`:null;const openPlayer=(channel:LiveTvChannel)=>{window.history.pushState({rpfLiveTvPlayer:true},"");setActive(channel)};const closePlayer=()=>{if(window.history.state?.rpfLiveTvPlayer)window.history.back();else setActive(null)};const preview=(channel:LiveTvChannel)=>{const id=channel.videoId||channel.url.match(/(?:youtu\.be\/|youtube\.com\/(?:live\/|watch\?v=))([^?&/]+)/)?.[1];return id?`https://i.ytimg.com/vi/${id}/hqdefault.jpg`:null};
return <div className="min-h-full bg-[#f7f9fc] pb-10"><div className="mx-auto max-w-5xl px-4 py-5 sm:px-6">{active?<div className="fixed inset-0 z-50 flex min-h-[100dvh] flex-col bg-black text-white"><div className="flex items-center justify-between gap-3 border-b border-white/10 px-4 py-3"><button onClick={closePlayer} className="inline-flex items-center gap-2 rounded-xl bg-white/10 px-3 py-2 text-xs font-bold text-white"><ArrowLeft className="h-4 w-4"/>{hi?"वापस":"Back"}</button><p className="min-w-0 flex-1 truncate text-center text-sm font-black">{active.name}</p><a className="inline-flex items-center gap-2 rounded-xl bg-white px-3 py-2 text-xs font-bold text-slate-900" href={active.url} target="_blank" rel="noreferrer"><ExternalLink className="h-4 w-4"/>{hi?"खोलें":"Open"}</a></div><div className="flex flex-1 items-center justify-center bg-black"><div className="w-full max-w-6xl aspect-video">{embedUrl?<iframe className="h-full w-full" src={embedUrl} title={active.name} allow="autoplay; encrypted-media; picture-in-picture; fullscreen" allowFullScreen/>:<div className="grid h-full place-items-center p-6 text-center text-white"><div><Tv className="mx-auto mb-3 h-10 w-10"/><p className="font-bold">{active.name}</p><a className="mt-4 inline-block rounded-xl bg-white px-4 py-2 text-sm font-bold text-slate-900" href={active.url} target="_blank" rel="noreferrer">Open stream</a></div></div>}</div></div><div className="border-t border-white/10 px-4 py-3 text-center"><p className="text-sm font-black">{active.name}</p><p className="mt-1 text-xs text-white/60">{active.category}{embedUrl?"":(hi?" • एम्बेड उपलब्ध नहीं":" • Embed unavailable")}</p></div></div>:<><div className="flex items-center gap-3"><div className="grid h-11 w-11 place-items-center overflow-hidden rounded-2xl bg-white ring-1 ring-slate-200 shadow-sm"><img src="/assets/logo.png" alt="RP Foundation" className="h-full w-full object-contain"/></div><div><h1 className="text-xl font-black text-[#000080]">{hi?"लाइव टीवी":"Live TV"}</h1><p className="text-xs text-slate-500">{visible.length} {hi?"चैनल उपलब्ध":"channels available"}{serverControlled?" • Live configuration":""}</p></div></div><div className="relative mt-5"><Search className="absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-slate-400"/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder={hi?"चैनल खोजें...":"Search channels..."} className="w-full rounded-2xl border border-slate-200 bg-white py-3 pl-10 pr-4 text-sm outline-none focus:border-[#000080] focus:ring-2 focus:ring-blue-50"/></div><div className="mt-5 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{filtered.map(channel=>{const thumb=preview(channel);return <button key={channel.id} onClick={()=>openPlayer(channel)} className="group overflow-hidden rounded-2xl border border-slate-200 bg-white text-left shadow-sm transition hover:-translate-y-0.5 hover:shadow-lg"><div className="relative aspect-video w-full overflow-hidden bg-slate-100">{thumb?<img src={thumb} alt="" loading="lazy" className="h-full w-full object-cover transition duration-300 group-hover:scale-[1.03]"/>:<div className="flex h-full items-center justify-center bg-gradient-to-br from-slate-100 to-slate-200"><Tv className="h-10 w-10 text-slate-400"/></div>}<span className="absolute inset-0 bg-gradient-to-t from-black/50 via-transparent to-transparent"/><span className="absolute bottom-3 left-3 grid h-10 w-10 place-items-center rounded-full bg-white/95 text-[#000080] shadow-lg"><Play className="h-4 w-4 fill-current"/></span><span className="absolute right-3 top-3 rounded-full bg-red-600 px-2 py-1 text-[9px] font-black uppercase tracking-wider text-white shadow">LIVE</span></div><div className="p-4"><p className="truncate text-sm font-black text-slate-900">{channel.name}</p><p className="mt-1 text-[11px] font-semibold text-slate-500">{channel.category}</p></div></button>})}</div>{!filtered.length&&<div className="py-16 text-center text-sm text-slate-500">{hi?"कोई चैनल नहीं मिला":"No channels found"}</div>}</>}</div></div>}
