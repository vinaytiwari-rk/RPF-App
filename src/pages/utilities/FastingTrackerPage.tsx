import React,{useEffect,useState}from"react";import{Clock3}from"lucide-react";import{useNavigate}from"react-router-dom";import Shell from"./UtilityPageShell";export default function FastingTrackerPage(){const n=useNavigate(),[start,setStart]=useState<number|null>(null),[now,setNow]=useState(Date.now());useEffect(()=>{if(!start)return;const id=window.setInterval(()=>setNow(Date.now()),1000);return()=>clearInterval(id)},[start]);const hours=start?(now-start)/36e5:0;return <Shell title="Fasting Tracker" icon={<Clock3 className="h-4 w-4"/>} onBack={()=>n("/utilities")}><div className="text-center"><div className="text-5xl font-black text-[#000080]">{hours.toFixed(2)}h</div><button className="mt-6 rounded-xl bg-[#000080] px-6 py-3 font-bold text-white" onClick={()=>setStart(start?null:Date.now())}>{start?"End Fast":"Start Fast"}</button></div></Shell>}
