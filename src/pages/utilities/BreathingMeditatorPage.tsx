import React,{useEffect,useState}from"react";import{Wind}from"lucide-react";import{useNavigate}from"react-router-dom";import Shell from"./UtilityPageShell";export default function BreathingMeditatorPage(){const n=useNavigate(),[run,setRun]=useState(false),[i,setI]=useState(0);const phases=["Inhale","Hold","Exhale","Hold"];useEffect(()=>{if(!run)return;const id=window.setInterval(()=>setI(v=>(v+1)%4),4000);return()=>clearInterval(id)},[run]);return <Shell title="Breathing Meditator" icon={<Wind className="h-4 w-4"/>} onBack={()=>n("/utilities")}><div className={`mx-auto flex h-40 w-40 items-center justify-center rounded-full border-4 border-indigo-200 bg-indigo-50 text-center text-lg font-black text-indigo-700 transition-transform duration-[4000ms] ${run&&i===0?"scale-125":"scale-100"}`}>{run?phases[i]:"Ready"}</div><button className="mx-auto mt-6 block rounded-xl bg-[#000080] px-6 py-3 font-bold text-white" onClick={()=>setRun(v=>!v)}>{run?"Stop":"Start"}</button></Shell>}
