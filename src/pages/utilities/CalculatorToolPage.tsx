import React from"react";import{useLocation,useNavigate}from"react-router-dom";import{ExternalLink,Calculator}from"lucide-react";import Shell from"./UtilityPageShell";
const special:Record<string,string>={"gfr-e-gfr-calculator":"gfr-calculator","sleep-health-calculator":"sleep-calculator","hba1c-converter":"hba1c-converter"};
export default function CalculatorToolPage(){const n=useNavigate(),{pathname}=useLocation();const slug=pathname.split("/").pop()||"";const title=slug.split("-").map(x=>x?x[0].toUpperCase()+x.slice(1):x).join(" ");const target=special[slug]||slug;const url=`https://www.calculator.net/${target}.html`;return <Shell title={title} icon={<Calculator className="h-4 w-4"/>} onBack={()=>n("/utilities/calculators")}><div className="space-y-4"><div className="rounded-2xl bg-slate-50 p-4"><h2 className="font-black text-slate-900">{title}</h2><p className="mt-1 text-sm text-slate-600">This tool opens inside RPF's in-app browser, so users remain inside the RPF app experience.</p></div><button onClick={()=>n(`/browser?url=${encodeURIComponent(url)}`)} className="flex w-full items-center justify-center gap-2 rounded-xl bg-[#000080] px-5 py-3 font-black text-white"><ExternalLink className="h-4 w-4"/>Open Calculator</button><p className="text-xs leading-relaxed text-slate-500">Health calculators provide estimates for awareness and screening support only. They do not diagnose disease or replace a qualified healthcare professional.</p></div></Shell>}
