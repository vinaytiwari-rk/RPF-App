import React,{useState}from"react";import{CheckCircle2}from"lucide-react";import{useNavigate}from"react-router-dom";import Shell from"./UtilityPageShell";export default function TypingSpeedPage(){const n=useNavigate(),sample="RPF Foundation community service makes a difference.",[x,setX]=useState(""),[start,setStart]=useState<number|null>(null);const wpm=start&&x.trim()?Math.round(x.trim().split(/\s+/).length/Math.max(1,(Date.now()-start)/6e4)):0;return <Shell title="Typing Speed Test" icon={<CheckCircle2 className="h-4 w-4"/>} onBack={()=>n("/utilities")}><p className="rounded-xl bg-slate-50 p-4 text-sm font-semibold">{sample}</p><textarea className="mt-4 min-h-28 w-full rounded-xl border p-3" value={x} onFocus={()=>!start&&setStart(Date.now())} onChange={e=>setX(e.target.value)} placeholder="Start typing..."/><div className="mt-4 text-center text-3xl font-black text-[#000080]">{wpm} WPM</div></Shell>}
