import React,{useState}from"react";import{Calculator}from"lucide-react";import{useNavigate}from"react-router-dom";import Shell from"./UtilityPageShell";export default function QuickCalculatorPage(){const n=useNavigate(),[x,setX]=useState("");const calc=()=>{try{if(!/^[0-9+\-*/().%\s]+$/.test(x))throw 0;setX(String(Function(`"use strict";return (${x})`)()))}catch{setX("Invalid")}};return <Shell title="Quick Calculator" icon={<Calculator className="h-4 w-4"/>} onBack={()=>n("/utilities")}><div className="flex gap-2"><input className="min-w-0 flex-1 rounded-xl border p-3" value={x} onChange={e=>setX(e.target.value)} placeholder="25*4+10"/><button className="rounded-xl bg-[#000080] px-5 font-black text-white" onClick={calc}>=</button></div></Shell>}
