import { useState, type ReactNode } from "react";
import { Bell, ChevronRight, Globe2, LogOut, Sparkles, UserRound, Volume2, Info, X } from "lucide-react";
import { motion } from "motion/react";
import { useNavigate, useOutletContext } from "react-router-dom";
import { toast } from "react-hot-toast";
import { useAuth } from "../context/AuthContext";
import { requestNotificationPermission } from "../lib/permissions";
import { vibrate } from "../lib/deviceCapabilities";

type Lang = "en" | "hi";
const key = "@rpf_user_settings";
function loadPrefs() { try { return JSON.parse(localStorage.getItem(key) || "{}") as Record<string, boolean>; } catch { return {}; } }

function DisclaimerModal({ close }: { close: () => void }) {
  return <div className="fixed inset-0 z-[100] overflow-y-auto bg-slate-950/45 p-3 backdrop-blur-sm"><div className="mx-auto my-3 max-w-2xl rounded-[28px] bg-white shadow-2xl"><div className="sticky top-0 flex items-center justify-between border-b border-slate-100 bg-white/95 px-5 py-4 backdrop-blur"><div className="flex items-center gap-2"><Info className="h-5 w-5 text-[#FF9933]"/><h2 className="text-base font-black text-[#000080]">About Samahit & External Links</h2></div><button onClick={close} className="rounded-xl p-2 text-slate-500 hover:bg-slate-100" aria-label="Close"><X className="h-5 w-5"/></button></div><article className="space-y-6 p-5 text-sm leading-6 text-slate-600 sm:p-7"><section><h3 className="font-black text-slate-900">Independent Community Initiative</h3><p className="mt-2">Samahit is an independent digital application developed and maintained voluntarily by individuals associated with the R.P. Foundation. It is intended to help users discover useful public information, services, digital resources and important external platforms from one place.</p><p className="mt-2">Samahit is not an official product or service of the R.P. Foundation and, unless explicitly stated otherwise, is not owned, operated, endorsed, sponsored or officially approved by the Foundation.</p></section><section><h3 className="font-black text-slate-900">Government Services and Links</h3><p className="mt-2">Samahit is not owned, operated, endorsed by, or officially affiliated with the Government of India, any State or Union Territory Government, Government Ministry, Department, Agency, Authority, Board or Public Institution.</p><p className="mt-2">Government links are included solely to help citizens conveniently discover and access publicly available welfare schemes, services and information through the respective official sources. The presence of a Government name, scheme, logo or link does not imply partnership, authorization, sponsorship or official association with Samahit.</p></section><section><h3 className="font-black text-slate-900">About External Links</h3><p className="mt-2">Samahit may contain links to external websites and resources that are not owned, operated, or controlled by Samahit or the R.P. Foundation. These links are provided for the convenience of users and to help them discover relevant information, services, resources, and platforms.</p><p className="mt-2">Samahit does not endorse, guarantee, or take responsibility for the content, accuracy, availability, security, privacy practices, policies, or services of any external website unless explicitly stated otherwise. Once a user accesses an external website, that website or service may be governed by its own terms, privacy policy, and practices.</p><p className="mt-2">Users are advised to exercise caution and independently review the policies, authenticity, and security of each external website before submitting personal, financial, identity, login, or other sensitive information.</p></section><section><h3 className="font-black text-slate-900">External Websites</h3><p className="mt-2">When an external website is opened, its content is served and controlled by the respective website or organization. Samahit does not host, modify or control that external content, availability, security or privacy practices. A website may be shown inside Samahit Browser for convenience, but this does not mean Samahit owns or operates that website.</p><p className="mt-2">Users should independently verify a website before entering personal, financial, identity, login or other sensitive information.</p></section><section><h3 className="font-black text-slate-900">Accuracy and Availability</h3><p className="mt-2">Government schemes, eligibility criteria, procedures, services, websites and policies may change. Users should verify important information directly with the concerned official source before making decisions or submitting applications. External websites may change, become unavailable or modify their content without notice.</p></section><section><h3 className="font-black text-slate-900">User Safety</h3><p className="mt-2">Check the website address before submitting information. Never share passwords, OTPs, banking details or sensitive credentials unless you are certain that you are interacting with a genuine and secure service.</p></section><section><h3 className="font-black text-slate-900">R.P. Foundation Association</h3><p className="mt-2">Samahit has been independently developed through voluntary efforts of individuals associated with the R.P. Foundation. Unless specifically stated otherwise, the R.P. Foundation does not own, operate, endorse, sponsor, approve or assume responsibility for the independent operation of the Samahit application.</p></section><p className="border-t border-slate-100 pt-4 text-xs text-slate-400">This information is provided to explain the independent nature of Samahit and the handling of government and other external links. It does not replace any applicable legal terms or rights.</p></article></div></div>;
}

export default function Settings() {
  const navigate = useNavigate(); const { lang } = useOutletContext<{ lang: Lang }>(); const { language, setLanguage, logout } = useAuth(); const hi = lang === "hi" || language === "hi";
  const initial = loadPrefs(); const [prefs, setPrefs] = useState({ notifications: initial.notifications !== false, dailyQuote: initial.dailyQuote !== false, haptics: initial.haptics !== false }); const [disclaimerOpen, setDisclaimerOpen] = useState(false);
  const save = (next: typeof prefs) => { localStorage.setItem(key, JSON.stringify(next)); setPrefs(next); };
  const toggle = (name: keyof typeof prefs) => save({ ...prefs, [name]: !prefs[name] });
  const toggleNotifications = async () => { if (prefs.notifications) { toggle("notifications"); return; } const status = await requestNotificationPermission(); if (status === "granted") { save({ ...prefs, notifications: true }); return; } toast.error(hi ? "Notification permission अनुमति नहीं मिली" : "Notification permission was not granted"); };
  const toggleHaptics = () => { const next = !prefs.haptics; save({ ...prefs, haptics: next }); if (next) vibrate(25); };
  const row = (icon: ReactNode, title: string, sub: string, action: ReactNode) => <div className="flex items-center gap-3 rounded-2xl border border-slate-100 bg-white p-3.5"><span className="flex h-10 w-10 items-center justify-center rounded-xl bg-slate-50 text-[#000080]">{icon}</span><span className="min-w-0 flex-1"><span className="block text-[12px] font-black text-slate-800">{title}</span><span className="mt-0.5 block text-[9px] leading-4 text-slate-500">{sub}</span></span>{action}</div>;
  const toggleButton = (on: boolean, click: () => void, active: string) => <button onClick={click} className={`h-6 w-11 rounded-full p-1 transition ${on ? active : "bg-slate-300"}`}><span className={`block h-4 w-4 rounded-full bg-white transition ${on ? "translate-x-5" : "translate-x-0"}`} /></button>;
  return <main className="min-h-full bg-[#f8fafc] pb-12"><div className="mx-auto max-w-3xl px-3.5 py-5 sm:px-6"><motion.section initial={{opacity:0,y:14}} animate={{opacity:1,y:0}} className="overflow-hidden rounded-[28px] border border-slate-200 bg-white shadow-sm"><div className="h-1.5 bg-gradient-to-r from-[#FF9933] via-[#7C3AED] to-[#138808]"/><div className="p-5 sm:p-7"><div className="flex items-center gap-3"><span className="flex h-12 w-12 items-center justify-center rounded-2xl bg-gradient-to-br from-orange-100 to-violet-100 text-[#000080]"><Sparkles className="h-6 w-6"/></span><div><p className="text-[9px] font-black uppercase tracking-[.2em] text-[#FF9933]">Samahit</p><h1 className="text-[23px] font-black text-[#000080]">{hi ? "सेटिंग्स" : "Settings"}</h1><p className="text-[10px] text-slate-500">{hi ? "ऐप को अपनी सुविधा के अनुसार बदलें" : "Personalize the app for your needs"}</p></div></div><div className="mt-6 space-y-2.5">
{row(<Globe2 className="h-5 w-5"/>, hi ? "भाषा" : "Language", hi ? "ऐप की भाषा" : "App language", <button onClick={() => setLanguage(language === "hi" ? "en" : "hi")} className="rounded-xl border border-slate-200 px-3 py-2 text-[10px] font-black text-[#000080]">{language === "hi" ? "English" : "हिन्दी"}</button>)}
{row(<Bell className="h-5 w-5"/>, hi ? "सूचनाएं" : "Notifications", hi ? "महत्वपूर्ण अपडेट और घोषणाएं" : "Important updates and announcements", toggleButton(prefs.notifications, () => void toggleNotifications(), "bg-[#138808]"))}
{row(<Sparkles className="h-5 w-5"/>, hi ? "दैनिक विचार" : "Daily Quote", hi ? "हर दिन Quote of the Day दिखाएं" : "Show the Quote of the Day", toggleButton(prefs.dailyQuote, () => toggle("dailyQuote"), "bg-[#7C3AED]"))}
{row(<Volume2 className="h-5 w-5"/>, hi ? "स्पर्श प्रतिक्रिया" : "Haptic Feedback", hi ? "जहां उपलब्ध हो वहां हल्का vibration" : "Use subtle vibration where supported", toggleButton(prefs.haptics, toggleHaptics, "bg-[#0EA5E9]"))}
{row(<Info className="h-5 w-5"/>, hi ? "समाहित और बाहरी लिंक" : "Samahit & External Links", hi ? "स्वतंत्र ऐप और सरकारी/बाहरी लिंक के बारे में" : "About the independent app and government/external links", <button onClick={() => setDisclaimerOpen(true)} className="rounded-xl bg-slate-50 p-2 text-slate-400"><ChevronRight className="h-4 w-4"/></button>)}
{row(<UserRound className="h-5 w-5"/>, hi ? "प्रोफाइल" : "Profile", hi ? "अपनी प्रोफाइल और पहचान अपडेट करें" : "Update your profile and identity", <button onClick={() => navigate("/profile")} className="rounded-xl bg-slate-50 p-2 text-slate-400"><ChevronRight className="h-4 w-4"/></button>)}
</div><button onClick={() => { void logout(); navigate("/"); }} className="mt-5 flex w-full items-center justify-center gap-2 rounded-2xl border border-red-200 bg-red-50 px-4 py-3.5 text-[12px] font-black text-red-600"><LogOut className="h-4 w-4"/>{hi ? "लॉग आउट" : "Log out"}</button></div></motion.section></div>{disclaimerOpen && <DisclaimerModal close={() => setDisclaimerOpen(false)} />}</main>;
}
